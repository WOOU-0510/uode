"use client";

const HomePage = () => {
  return (
    <main className="container">
      <h1>uode</h1>
      <p>홈 화면 (IPC 데모는 제거됨 — Rust 커맨드는 notes / reports 쪽에 추가 예정)</p>
    </main>
  );
};

export default HomePage;
